<?php

class ConfSwitches extends Eloquent{

	/**
	 * The database table used by the model.
	 *
	 * @var string
	 */
    protected $connection = 'mysql';
	protected $table = 'noglpi_confclientes_switches';
    
}